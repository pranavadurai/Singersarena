<div id="messages" data-conversation-id = "<%= @conversation.id %>" >
  <div class="" id="message_contain_box">
    <% @messages.each do |message| %>
     <% if message.body %>
       <% if message.user_id == current_user.id %>
         <p class="text-right"><span class="badge badge-pill badge-info"><%= message.body %></span></p>
       <% else %>
         <p class="text-left"><span class="msg badge badge-pill badge-warning"><%= message.body %></span> </p>
       <% end %>
      <% end %>
    <% end %>
  </div>
   <%= form_for [@conversation, @message],remote: true, html: {class: "row"} do |f| %>
    <div class="col-10">
      <%= f.text_area :body, class: "form-control" %>
    </div>
    <%= f.text_field :user_id, value: current_user.id, type: "hidden" %>
    <div class="col-2">
      <%= f.submit "Send", class: "btn btn-primary" %>
    </div>
   <% end %>
 </div>
