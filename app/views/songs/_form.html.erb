<%= form_for(song) do |f| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="col-md-12">
      <div class="row">
        <div class="col-md-10 form-group">
        <%= f.text_field :title,class: "form-control",placeholder: "Title",:required => true %>
        </div>
        <div  class="form-group col-md-2 col-sm-2">
        <%= f.text_field :category,class: "form-control",placeholder: "Tag",:required => true %>
      </div>
    </div>
      <div class="row">
        <div class="form-group col-md-7">
          <%= f.file_field :songs,class: "btn btn-default",placeholder: "Song:",:required => true %>
        </div>
        <div class=" form-group col-md-5">
        <%= f.text_field :language,class: "form-control",placeholder: "Language",:required => true %>
        </div>
      </div>

     <div class="row justify-content-md-center ">
      <%= f.submit "Upload",class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
